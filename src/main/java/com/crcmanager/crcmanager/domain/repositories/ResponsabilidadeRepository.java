package com.crcmanager.crcmanager.domain.repositories;

import com.crcmanager.crcmanager.domain.models.Responsabilidade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResponsabilidadeRepository extends JpaRepository<Responsabilidade, Long> {
}